using UnityEngine;

public class CItem_Weapon : CItem
{

    // Weapon specific properties
    public int damage;
    public float range;
    public float attackSpeed;
    // Add any additional weapon-specific properties or methods here

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    
}
